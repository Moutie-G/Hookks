import recat,{useEffect, UseState} from "react" ;




function useInterval(timeOut, getValue){
    const [avlue, SetValue] = useState(getValue);
    useEffect(()=>{
        const intervallId=SetInterval(()
    }

    )
}